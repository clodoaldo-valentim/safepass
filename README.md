### Segurança de Dados

Usamos a biblioteca `cryptography` para transformar a senha em um **"código alienígena"** que só pode ser lido se o usuário tiver a chave correta.

No terminal, instale a dependência necessária:
pip install cryptography
